void main() {

    // Instanciando um carro 
    Carro carro = Carro("ABC1234", 123456);

    // Instanciando um animal 
    Animal animal = Animal(1.5, "Marrom");

    // Instanciando uma consulta 
    Consulta consulta = Consulta(DateTime.now(), "João", "Dr. Silva");

}